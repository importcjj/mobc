language: rust
rust:
  # - stable
  # - beta
  - nightly

matrix:
  allow_failures:
    - rust: stable
  fast_finish: true

cache: cargo
script:
  - cargo build --verbose --all
  - cargo test --verbose --all --features async-std-runtime
  - cargo test --verbose --all --features tokio-runtime
  # - cargo bench